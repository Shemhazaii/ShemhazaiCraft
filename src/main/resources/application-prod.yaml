spring:
  data:
    redis:
      host: redis.messaging.svc.cluster.local
      port: 6379
      password:
      database: 2
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
  datasource:
    url: jdbc:postgresql://mypostgres.database.svc.cluster.local:5432/shemhazaicraft_prod
    username: postgres
    password: Dimas123
    hikari:
      auto-commit: true
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
        jdbc:
          lob:
            non_contextual_creation: true
    open-in-view: false
  sql:
    init:
      mode: always

minio:
  url: http://minio.database.svc.cluster.local:9000
  access-key: loki
  secret-key: supersecret
  bucket-name: shemhazaicraft-prod
